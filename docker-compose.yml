version: '3.8'

services:
    web:
        build: .
        command: sh -c "uvicorn blog_fast_api_python.main:app --host 0.0.0.0 --port 8000"
        ports:
            - "8000:8000"
        volumes:
          - .:/app